from setuptools import setup

setup(name='flow_runner',
      version='0.1',
      description='A module to run a flow from a fsm definition',
      url='https://github.com/ekarpovs/flow_runner',
      author='ekarpovs',
      author_email='',
      license='MIT',
      packages=['flow_runner', 'frfsm'],
      zip_safe=False)